  function sumCheck(){
      var x=parseInt(prompt("Enter a number"));
      var y=parseInt(prompt("Enter a number"));
      var sum=x+y;
      var display=`${sum} is the required value`
      document.getElementById("sumEx")
      .innerHTML=display;

//using template literal(it basically allows mixing varibles numbers and more,like we did previously to print nultipliction table)
//table of 10
for(let i = 1;i<11;i++)
{
    console.log(`10 X ${i} = ${10*i}`);  //--this: ${} is actually template literal
}